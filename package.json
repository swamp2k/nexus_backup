{
  "name": "nexus-backup",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "clean": "tsc -b --clean",
    "build": "tsc -b --pretty false",
    "check:js": "node --check apps/local-server/bin/server.mjs && node --check apps/local-server/lib/dashboard-data.mjs && node --check apps/local-server/lib/runtime-telemetry.mjs && node --check apps/local-server/lib/backup-plans.mjs && node --check apps/local-server/web/app.js && node --check apps/local-server/web/telemetry.js && node --check apps/local-server/web/plans.js",
    "test": "npm run clean && npm run build && npm run check:js && node --test packages/core/test/*.test.mjs apps/agent/test/*.test.mjs apps/control-plane/test/*.test.mjs apps/local-server/test/*.test.mjs",
    "typecheck": "tsc -b --pretty false",
    "start:agent": "node apps/agent/bin/agent.mjs",
    "start:local": "node apps/local-server/bin/server.mjs"
  }
}
